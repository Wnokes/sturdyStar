  	<div class='page-title'>
  		<h1>Portfolios of Previous Work</h1>

      <% if logged_in?(:admin) %> 
  		  <%= link_to "New Portfolio", new_portfolio_path, class: "btn btn-outline-dark" %>
      <% end %>

  	</div>
<div class="album text-muted">
  <div class="container">

    <div class="row portfolio-item sortable">
      <%= render partial: 'portfolio_item', collection: @portfolios %>
    </div>
  </div>
</div>

